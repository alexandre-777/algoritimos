programa {
  funcao inicio() {
    //declarção de variáveis
    inteiro numero

    //coleta de dados
    escreva("digite o su némero:")
    leia(numero)

    //processamento de dados
   se(numero % 2 == 0) {
    escreva("este número é par.")
   } senao {
   escreva("este número é ímpar.")}
  }
}
